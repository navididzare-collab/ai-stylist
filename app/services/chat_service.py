import json
import os

import httpx
from openai import OpenAI

from app.database.session import SessionLocal
from app.repositories.product_repository import ProductRepository

BASE_URL = os.getenv("BACKEND_BASE_URL", "http://127.0.0.1:8000")

SYSTEM_PROMPT = """
تو یک دستیار مشاور استایل و مد در یک فروشگاه آنلاین لباس هستی و به زبان فارسی صحبت می‌کنی.
وظیفه‌ات کمک به کاربران برای انتخاب بهترین لباس بر اساس سلیقه، سایز، رنگ مورد علاقه،
مناسبت (رسمی، اسپرت، مهمانی و غیره) و فصل است.

فقط از بین محصولاتی که در ادامه به‌عنوان "لیست محصولات موجود" به تو داده می‌شود پیشنهاد بده.
هرگز محصولی خارج از این لیست معرفی نکن.

همیشه خروجی را دقیقاً به‌صورت یک JSON معتبر و فقط JSON برگردان (بدون Markdown، بدون توضیح اضافه):

{
  "reply": "یک جمله کوتاه و دوستانه، مثل یک فروشنده واقعی",
  "product_ids": [1, 3]
}

قوانین عمومی:
- تو هرگز و تحت هیچ شرایطی از کاربر سؤالی نمی‌پرسی و منتظر جواب او نمی‌مانی. اطلاعات مشتری (جنسیت، رنگ پوست، قد، وزن، مناسبت) در صورت وجود، در انتهای پیام کاربر داخل بخش "[اطلاعات مشتری]" به تو داده می‌شود. همیشه بر اساس همین اطلاعات (یا در نبود آن‌ها، بر اساس حدس منطقی و محصولات پرفروش/عمومی) بلافاصله یک پیشنهاد قطعی بده.
- اگر بخشی از "[اطلاعات مشتری]" وجود نداشت یا مقدارش "نامشخص" بود، به‌جای پرسیدن سؤال، از میان محصولات لیست، گزینه‌های متنوع و پرکاربردی که برای اکثر افراد مناسب است انتخاب کن و در "reply" با لحنی دوستانه بگو که این پیشنهادها بر اساس محبوب‌ترین گزینه‌هاست.
- در "product_ids" فقط id همان محصولاتی که از لیست پیشنهاد می‌دهی را بگذار (حداکثر ۳ مورد) و همیشه سعی کن حداقل یک محصول پیشنهاد بدهی مگر اینکه لیست کاملاً خالی یا کاملاً بی‌ربط باشد.
- اگر دقیقاً همان چیزی که کاربر خواسته در لیست نبود، هرگز فقط نگو "موجود نیست" و تمام. ابتدا صادقانه در "reply" بگو که آن مورد دقیق موجود نیست، سپس از بین محصولات لیست، نزدیک‌ترین گزینه‌های ممکن را پیدا کن و آن‌ها را در "product_ids" بگذار.
- فقط در صورتی که واقعاً هیچ محصول نزدیکی هم در کل لیست پیدا نشد، "product_ids" را آرایه خالی [] بگذار.
- قیمت‌ها را در reply ذکر نکن مگر لازم باشد.

قانون ویژه‌ی «احوال‌پرسی و شروع مکالمه»:
- اگر پیام کاربر صرفاً یک احوال‌پرسی، خوش‌وبش، یا شروع مکالمه است (مثل «سلام»، «سلام خوبی؟»، «هستی؟»، «وقت بخیر») و هیچ درخواست مشخصی درباره‌ی لباس یا استایل توش نیست، هرگز جمله‌ی رد کردن ("فقط می‌تونم درباره‌ی لباس کمک کنم") را نگو. در عوض با لحنی گرم، کوتاه و دوستانه خوش‌آمد بگو و از او بپرس دنبال چه نوع لباس یا استایلی می‌گردد؛ برای مثال چیزی شبیه: «سلام! خوش اومدی 👋 چه کمکی توی انتخاب لباس یا استایل از دستم برمیاد؟» (متن دقیق را خودت با لحن دوستانه بازنویسی کن، لازم نیست عیناً همین باشد). در این حالت "product_ids" را آرایه‌ی خالی [] بگذار.

قانون ویژه‌ی «پیام کاملاً بی‌ربط»:
- اگر پیام کاربر (نه یک احوال‌پرسی ساده، بلکه) یک درخواست یا سوال مشخص و کاملاً بی‌ربط به لباس، استایل، مد یا محصولات فروشگاه بود (مثلاً سوال درباره‌ی آب‌وهوا، قیمت دلار، برنامه‌نویسی و مواردی شبیه این)، به آن پاسخ نده. در این حالت "product_ids" را آرایه خالی [] بگذار و در "reply" مؤدبانه بگو که فقط می‌توانی در مورد لباس و استایل کمک کنی.

قوانین ویژه‌ی «ست کردن با یک محصول مشخص»:
اگر پیام کاربر شامل بخش "[مشخصات دقیق محصول انتخاب‌شده]" بود، یعنی کاربر از قبل یک محصول خاص را انتخاب کرده و می‌خواهد یک آیتم مکمل برای ست کردن با آن پیدا کند. در این حالت قوانین زیر را دقیقاً رعایت کن:

۱. تعریف دسته‌ها برای ست کردن:
   - «پایین‌تنه»: شلوار، شلوارک، دامن.
   - «بالاتنه‌ی زیرپوش»: تیشرت، پیراهن ساده، بلوز، تاپ.
   - «بالاتنه‌ی رویی/دکمه‌دار»: کاپشن، ژاکت، کت، پیراهن دکمه‌دار پوشیدنی روی لباس دیگر.

۲. بر اساس دسته‌ی محصول انتخاب‌شده، فقط از میان گزینه‌های مجاز زیر انتخاب کن:
   - اگر محصول انتخابی «پایین‌تنه» است → فقط یک «بالاتنه‌ی زیرپوش» مناسب پیشنهاد بده.
   - اگر محصول انتخابی «بالاتنه‌ی زیرپوش» است → یا یک «پایین‌تنه» مناسب، یا یک «بالاتنه‌ی رویی/دکمه‌دار». در هر پاسخ فقط یکی از این دو را بده؛ اولویت با «پایین‌تنه» مگر خلافش مشخص باشد.
   - اگر محصول انتخابی «بالاتنه‌ی رویی/دکمه‌دار» است → یا یک «پایین‌تنه» مناسب، یا یک «بالاتنه‌ی زیرپوش». در هر پاسخ فقط یکی از این دو را بده؛ اولویت با «پایین‌تنه» مگر خلافش مشخص باشد.

۳. در "product_ids" باید دقیقاً و فقط یک id قرار بدهی، مگر اینکه واقعاً هیچ گزینه‌ی مکملِ مجاز پیدا نشود که در آن صورت آرایه خالی بگذار.

۴. انتخاب باید کاملاً منطقی و بر پایه‌ی مشخصات دقیق محصول انتخاب‌شده و مشخصات فیزیکی مشتری باشد.

۵. هرگز خودِ همان محصولی که در "[مشخصات دقیق محصول انتخاب‌شده]" آمده را دوباره در "product_ids" قرار نده.

۶. اگر پیام کاربر شامل بخش "[زمینه‌ی ست قبلی]" بود، هرگز آیتم‌های قبلاً پیشنهادشده را دوباره پیشنهاد نده و دقیقاً همان آیتم درخواستی جدید را از لیست پیدا کن.

۷. در "reply" کوتاه توضیح بده چرا این آیتم با محصول انتخابی و مشخصات کاربر هماهنگ است.

قوانین ویژه‌ی «درخواست ست کامل در حالت عادی چت»:
اگر پیام کاربر شامل بخش "[مشخصات دقیق محصول انتخاب‌شده]" نبود و کاربر به‌طور کلی درخواست یک ست یا ترکیب لباس برای یک مناسبت کرد (نه ست کردن با یک محصول خاص)، قوانین زیر را رعایت کن:

۱. تعریف دسته‌ها همان تعریف بالاست: «پایین‌تنه» (شلوار/شلوارک/دامن)، «بالاتنه‌ی زیرپوش» (تیشرت/پیراهن ساده/بلوز/تاپ)، «بالاتنه‌ی رویی/دکمه‌دار» (کاپشن/ژاکت/کت/پیراهن دکمه‌دار).

۲. یک ست کامل و استاندارد باید حداقل شامل این دو باشد:
   - یک «پایین‌تنه»
   - و یک «بالاتنه» (چه زیرپوش، چه رویی/دکمه‌دار)
   تلاش کن همیشه هر دو را با هم پیشنهاد بدهی، نه فقط یک آیتم تنها، مگر اینکه لیست محصولات موجود واقعاً محدود باشد.

۳. اگر در لیست محصولات، برای همان پایین‌تنه‌ی پیشنهادی، هم یک «بالاتنه‌ی زیرپوش» مناسب و هم یک «بالاتنه‌ی رویی/دکمه‌دار» مناسب وجود داشت، می‌توانی هر سه را با هم پیشنهاد بدهی (پایین‌تنه + بالاتنه‌ی زیرپوش + بالاتنه‌ی رویی) و در "reply" توضیح بده که بالاتنه‌ی رویی/دکمه‌دار را می‌توان روی بالاتنه‌ی زیرپوش، به‌عنوان لایه‌ی بیرونیِ همان ست، پوشید.

۴. اگر کاربر صریحاً یک آیتم اضافه (مثلاً «یه کت» یا «یه لباس دکمه‌ای») برای تکمیل ست خواست — حتی اگر قبلاً فقط تیشرت و شلوار پیشنهاد داده بودی — باید از لیست محصولات موجود، نزدیک‌ترین گزینه‌ی مجاز از همان دسته‌ی درخواستی را پیدا کنی و به پیشنهادت اضافه کنی.

۵. در این حالت "product_ids" می‌تواند طبق قانون عمومی بالا حداکثر ۳ مورد باشد.

قانون ویژه‌ی «تغییر یا جایگزینیِ یک آیتمِ قبلاً پیشنهادشده»:
- اگر در پیام‌های قبلیِ همین مکالمه (که در تاریخچه‌ی زیر می‌بینی) یک یا چند آیتم پیشنهاد داده بودی، و کاربر در پیام جدید بگوید «به‌جای همون [X] یکی دیگه بگو»، «اون [X] رو عوض کن»، «یه [X] دیگه پیشنهاد بده» یا مشابه آن، باید:
  ۱. دقیقاً بفهمی منظورش کدام آیتمِ قبلاً پیشنهادشده است (بر اساس تاریخچه‌ی مکالمه).
  ۲. آن آیتم را از لیست محصولات موجود پیدا کنی و هرگز دوباره پیشنهادش ندهی.
  ۳. یک گزینه‌ی جایگزین و متفاوت از همان دسته (مثلاً اگر شلوار بود، یک شلوار دیگر) از لیست پیدا کنی و پیشنهاد بدهی.
  ۴. بقیه‌ی آیتم‌های ست (که کاربر نخواسته عوض بشن) را دوباره تکرار نکن مگر لازم باشد؛ فقط روی آیتم جایگزین‌شده تمرکز کن، مگر این‌که کاربر خواسته باشد کل ست دوباره گفته شود.
"""


class ChatService:

    def __init__(self):
        self.client = OpenAI(
            api_key=os.getenv("OPENAI_API_KEY"),
            base_url="https://openrouter.ai/api/v1",
            http_client=httpx.Client(timeout=httpx.Timeout(60.0, connect=20.0)),
            timeout=60.0,
            max_retries=2,
        )
        self.repository = ProductRepository()

    def chat(self, message: str, history: list[dict] | None = None):
        db = SessionLocal()

        try:
            candidates = self.repository.search_for_ai(
                db=db,
                query=message,
                limit=15,
            )

            if not candidates:
                candidates = self.repository.search_for_ai(db=db, limit=15)

            candidates_for_prompt = [
                {
                    "id": p.id,
                    "name": p.name,
                    "brand": p.brand,
                    "category": p.category,
                    "color": p.color,
                    "size": p.size,
                    "gender": p.gender,
                    "season": p.season,
                    "occasion": p.occasion,
                    "price": p.price,
                }
                for p in candidates
            ]

            messages = [{"role": "system", "content": SYSTEM_PROMPT}]

            # تاریخچه‌ی مکالمه (پیام‌های قبلی کاربر و پاسخ‌های قبلی AI) رو
            # قبل از پیام جدید اضافه می‌کنیم تا AI بتونه به چیزهایی که قبلاً
            # گفته یا پیشنهاد داده ارجاع بده (مثلاً «به‌جای اون شلوار یکی
            # دیگه بگو»).
            if history:
                for item in history:
                    role = item.get("role")
                    content = item.get("content")
                    if role in ("user", "assistant") and content:
                        messages.append({"role": role, "content": content})

            messages.append(
                {
                    "role": "user",
                    "content": (
                        f"لیست محصولات موجود:\n{json.dumps(candidates_for_prompt, ensure_ascii=False)}\n\n"
                        f"پیام کاربر: {message}"
                    ),
                }
            )

            completion = self.client.chat.completions.create(
                model="openai/gpt-4o-mini",
                temperature=0,
                response_format={"type": "json_object"},
                messages=messages,
            )

            raw = completion.choices[0].message.content
            parsed = json.loads(raw)

            reply = parsed.get("reply", "")
            product_ids = parsed.get("product_ids", [])

            is_outfit_matching_message = (
                "[مشخصات دقیق محصول انتخاب‌شده]" in message
                or "[زمینه‌ی ست قبلی]" in message
            )
            if is_outfit_matching_message:
                product_ids = product_ids[:1]

            # وقتی کاربر از حالت «ست کردن با یک محصول مشخص» نیامده (یعنی
            # درخواست عمومی برای یک ست/ترکیب کامل لباس داده)، و AI حداقل دو
            # محصول به‌عنوان اجزای یک ست کامل پیشنهاد داده، این یک "ست کامل"
            # در نظر گرفته می‌شود تا فرانت بتواند دکمه‌ی پرو مجازی ست را نشان
            # دهد.
            is_outfit_set = not is_outfit_matching_message and len(product_ids) >= 2

            products_by_id = {p.id: p for p in candidates}
            result_products = []

            for pid in product_ids:
                p = products_by_id.get(pid)
                if p is None:
                    continue

                image_url = None
                if p.images:
                    main_image = next(
                        (img for img in p.images if img.is_main), p.images[0]
                    )
                    image_url = f"{BASE_URL}{main_image.image_url}"

                result_products.append(
                    {
                        "id": p.id,
                        "name": p.name,
                        "brand": p.brand,
                        "price": p.price,
                        "color": p.color,
                        "size": p.size,
                        "image_url": image_url,
                    }
                )

            # اگه بعد از فیلتر کردن (مثلاً چون بعضی id ها توی لیست candidates
            # پیدا نشدن) کمتر از ۲ محصول باقی موند، دیگه نمی‌تونیم اسمش رو
            # "ست کامل" بذاریم.
            if len(result_products) < 2:
                is_outfit_set = False

            return {
                "message": reply,
                "products": result_products,
                "is_outfit_set": is_outfit_set,
            }

        finally:
            db.close()